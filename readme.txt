This is just a test task. The solution is massively inspired by gem 'Hashie' tricks.

In order to run rspec you should install the gem:

> gem install rspec
> gem install active_support

and run rspec:

> rspec

