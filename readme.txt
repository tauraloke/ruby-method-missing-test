This is just a test task.

The solution for Part #3 is massively inspired by gem 'Hashie' tricks.

I don't care about singular grammatics and got it from gem ActiveSupport.

In order to run rspec you should install these gems:

> gem install rspec activesupport

and run rspec:

> rspec

